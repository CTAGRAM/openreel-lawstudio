# OpenReel — Community Announcement (Discord + X)

June 2026 — **Desktop public alpha is live** · **Android 1.0.8 + iOS 1.2 in review**

> **Reference facts (don't over-claim):**
> - **Desktop** `v1.0.0-alpha.6` — macOS (Apple Silicon + Intel), Windows, Linux. Free, no watermarks. macOS build is signed + notarized; auto-updates built in. Download via **openreel.video** (installers served from dl.openreel.video).
> - **Web** — full editor at **app.openreel.video** (Chrome/Edge), live now.
> - **Android 1.0.8** + **iOS 1.2** — submitted, **in review** (not yet live). Don't say "available now" for mobile; say "rolling out / in review."
> - Cross-platform, same project format. Open source. Fill in your GitHub + store URLs where marked `<...>`.

---

## DISCORD ANNOUNCEMENT

> Post in #announcements. Replace `@everyone` with your preferred role ping, and fill the `<links>`.

```
@everyone

🚀 **OpenReel Desktop is here (public alpha) — and our biggest mobile update is in review!**

We've been heads-down shipping, and today's a big one. OpenReel is now a true **everywhere editor** — browser, desktop, and mobile, with the same project format across all three. No watermarks. No subscription wall. Open source.

🖥️ **NEW — OpenReel Desktop (public alpha)**
Native apps for **macOS (Apple Silicon + Intel), Windows, and Linux**:
• Full **cloud-GPU AI toolkit** — upscale, denoise, face restore, colorize, object removal, auto-captions, scene detection, object tracking, voice enhance, and audio separation — running on dedicated GPU servers so your machine stays fast.
• **Multi-track timeline**, 30+ filters/effects, new transitions, redesigned inspector.
• **One-click "Match Source" export** — render at your source video's exact dimensions, instantly.
• Signed + notarized on macOS, with built-in auto-updates.
👉 Download: **openreel.video**

📱 **IN REVIEW — Android 1.0.8 & iOS 1.2 (our biggest mobile update yet)**
• **AI Photo Tools** — erase unwanted objects, replace areas with AI, and expand shots beyond the frame (generative outpaint), plus denoise + color match.
• **Reliable cloud AI** — background removal, upscaling, face restore, audio separation, scene detection.
• **Pro audio** — broadcast-standard loudness (LUFS) + audio-only export.
• **Text-to-speech voiceover** and a new **full-screen crop editor** (iOS).
These are in store review right now — we'll ping the second they go live on the Play Store and App Store.

🌐 **Prefer the browser?** The full editor runs at **app.openreel.video** today.

💬 **We need your feedback.** This is alpha — tell us what breaks, what's missing, and what you want next in #feedback.
⭐ If you love where this is going, star us on GitHub (<github-url>) and share it — it genuinely helps.

Let's build the editor creators actually deserve. 🎬
```

---

## X / TWITTER

### Option A — Single launch tweet (standalone)

```
🚀 OpenReel Desktop is here — free, no watermarks, open source.

Native apps for macOS, Windows & Linux with a full cloud-GPU AI toolkit. And our biggest mobile update (Android + iOS) is in review.

The everywhere video editor → openreel.video
```

### Option B — Thread (each tweet ≤280 chars)

```
1/ 🚀 OpenReel Desktop is here — free, no watermarks, open source.

Native apps for macOS, Windows & Linux. Plus our biggest mobile update (Android + iOS) is in review.

The everywhere video editor. ⬇️
openreel.video
🧵
```

```
2/ 🖥️ Desktop (public alpha): multi-track timeline, 30+ effects, and a full AI toolkit — upscale, denoise, face restore, object removal, auto-captions, scene detection, voice enhance — all GPU-accelerated so your machine stays fast.

mac (M-series + Intel), Windows, Linux.
```

```
3/ ⚡ One-click "Match Source" export: render at your clip's exact dimensions instantly — no hunting through resolution presets.
```

```
4/ 📱 In review now — Android 1.0.8 + iOS 1.2:

AI Photo Tools — erase objects, replace with AI, expand beyond the frame (outpaint), + denoise & color match. Plus LUFS audio loudness, text-to-speech voiceover, and a new crop editor.

Rolling out on Play Store + App Store soon.
```

```
5/ 🌐 Don't want to install anything? The full editor runs in your browser: app.openreel.video

Same project format across web, desktop & mobile.
```

```
6/ No watermarks. No subscription wall. Open source.

We're building the video editor creators actually deserve — and we want your feedback.

Try it → openreel.video
⭐ + RT if you're rooting for an open alternative.
```

### Suggested hashtags (pick 2–3, don't stuff)

```
#videoediting #opensource #contentcreator #videoeditor #CapCutAlternative
```

---

## Posting tips

- **Discord first**, then X — drop the X thread link back in Discord so the community can boost it.
- Attach media: a short screen-recording of the desktop app + an AI Photo Tools before/after. Visual posts dramatically outperform text-only.
- For X, pin Option B tweet 1/ to your profile during launch week.
- When mobile clears review, post a short follow-up ("✅ now live on the Play Store / App Store") with the direct store links.
- Replace every `<...>` placeholder (GitHub URL, store links) before posting.
